# fundeploy 是纯 Bash 项目。此前这里是本仓库改名前（Python 时期）遗留的模板：
# 既忽略了一堆本仓库不会产生的东西，又漏掉了实际会产生的构建产物。
# 尤其注意旧模板里的 `/lib/` —— 它会忽略顶层 lib/ 目录，对本仓库是误伤风险。

# ── 构建与发布产物 ────────────────────────────────────────────────
dist/
# packaging/build-apt-repo.sh 的输出目录。该脚本在目录非空时会硬失败，
# 因此残留的本地 public/ 既是噪声也是坑。
public/
*.deb
fundeploy-*.tar.gz
# 生成的 Homebrew formula
fundeploy.rb
__pycache__/
*.pyc
*.db
*.rar
.venv/
.run/
node_modules/

# ── 日志 ─────────────────────────────────────────────────────────
logs/
*.log
*.log.gz

# ── 编辑器 / IDE ─────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── 操作系统 ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── 外部工具留下的缓存 ───────────────────────────────────────────
# .pytest_cache 并非本仓库测试产生（本仓库无 Python），而是外部 funbuild 工具留下的。
.pytest_cache/

# ---- AI 编码助手 ----
# 只忽略本地状态；团队有意共享的 agent / 命令 / 规则定义仍然版本化。
# 必须写 `dir/*` 而不是 `dir/`：父目录被整体排除后 git 不会下探，
# 下面的 `!` 取消规则就永远不会生效。
.claude/*
!.claude/agents/
!.claude/commands/
!.claude/skills/
!.claude/settings.json
.codex/*
!.codex/prompts/
.cursor/*
!.cursor/agents/
!.cursor/rules/

# 其余助手：目前没有共享内容，整体忽略
.aider*
.gemini/
.continue/
.windsurf/
.roo/
.cline/
.specstory/
CLAUDE.local.md
AGENTS.local.md
